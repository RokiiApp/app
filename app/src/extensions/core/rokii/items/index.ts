import { quit } from "./quit";
import { reload } from "./reload";
import { version } from "./version";
import { openSettings } from "./settings";

export const items = [openSettings, quit, reload, version];
